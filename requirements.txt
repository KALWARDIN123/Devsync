Django>=4.2,<5.0
# Django REST Framework for API endpoints
djangorestframework>=3.14,<4.0
# Celery for async tasks
celery>=5.2,<6.0
# For email sending (Django has built-in support, but add if using SMTP backends)
# django-anymail (optional, for 3rd party email services)
# Pillow for image/avatar uploads
Pillow>=9.0,<11.0
# For select2 widget in forms (if used)
django-select2>=8.0,<9.0
# For handling CORS in API (optional)
django-cors-headers>=3.13,<4.0
# For PostgreSQL (if using as DB)
# psycopg2-binary>=2.9,<3.0
# For Redis as Celery broker (if using)
# redis>=4.0,<5.0
# For Bootstrap (frontend, usually via CDN, not pip)
# For HTMX (frontend, via CDN)
# For jQuery (frontend, via CDN)

channels>=4.0.0
daphne>=4.0.0
openai>=1.0.0
python-dotenv>=1.0.0
requests>=2.31.0
python-dateutil>=2.8.2
beautifulsoup4>=4.12.0
markdown>=3.5.0
django-htmx>=1.17.0
whitenoise>=6.5.0
gunicorn>=21.2.0
django-tailwind>=3.6.0 